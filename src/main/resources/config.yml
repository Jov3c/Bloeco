currency:
  name: "金币"
  singular: "金币"
  plural: "金币"
  symbol: "⛃"
storage:
  type: sqlite
  sqlite:
    file: economy.db

# Redis is an optional, rebuildable accelerator. It is never the source of truth.
redis:
  enabled: false
